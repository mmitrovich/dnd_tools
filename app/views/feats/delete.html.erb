<% 
	@page_title = "Confirm Delete" 
	@menu_item = "Feats"
%>


<% content_for :sidebar do %>
	<p>Actions:</p>
	<p>
		<%= link_to("Cancel", feat_path(@feat), :class => 'btn-red') %>
	</p>
<% end %>

<% content_for :main_content do %>
		<div style="text-align: center;">
			<%= form_for(@feat, :method => 'delete') do |f| %>
				<h2>
					Are you sure you want to delete this feat:
				</h2>
				<h2>
					<%= f.object.name %>?
				</h2>
				<h2>
					Note:  This cannot be undone!
				</h2>
				<p>
					<%= f.submit("Yes, delete!", :class => 'btn-green') %>
				</p>
			<% end %>
		</div>
<% end %>